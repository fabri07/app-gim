# Python
__pycache__/
*.py[cod]
*.egg-info/
.Python

# Entorno virtual
.venv/
venv/
env/

# Base de datos local (SQLite). El esquema vive en las migraciones, no acá.
db.sqlite3
db.sqlite3-journal

# Django
*.log
local_settings.py

# Logs de la app (canal de auditoría, no se versiona)
logs/*.log

# Variables de entorno / secretos
.env

# Uploads locales de dev (logo, comprobantes). En producción van a R2, no acá.
media/

# Node (solo se usa para compilar Tailwind -- ver package.json). El output
# compilado (static/css/app.css) SÍ se versiona: Render no corre npm.
node_modules/

# Editores / SO
.DS_Store
.idea/
.vscode/
