Django==5.2.15
pillow==12.3.0
python-dotenv==1.2.2

# Fase 5 (producción / Render + Cloudflare R2)
boto3==1.43.38
dj-database-url==3.1.2
django-storages==1.14.6
gunicorn==26.0.0
psycopg[binary]==3.3.4
whitenoise==6.12.0

# Parte C (integración Google Calendar del alumno)
google-auth==2.55.2
google-auth-oauthlib==1.4.0
google-api-python-client==2.198.0
cryptography==48.0.1

# Proyecto 2 (importador de planes desde Excel)
openpyxl==3.1.5
rapidfuzz==3.13.0

# Descarga de PDF de rutina asignada (rutinas/pdf.py)
fpdf2==2.8.8

# PWA / Web Push (app notificaciones) -- trae py-vapid transitivo, con el
# comando `vapid` para generar el par de claves VAPID
pywebpush==2.4.0
